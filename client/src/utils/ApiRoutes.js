export const HOST = "http://localhost:3005"; // Define the HOST variable

const AUTH_ROUTE = `${HOST}/api/auth`; // Define the AUTH_ROUTE variable

export const CHECK_USER = `${AUTH_ROUTE}/check-user`; // Define the CHECK_USER variable